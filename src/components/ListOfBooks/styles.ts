import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  listBooksContainer: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
    backgroundColor: "#eee",
    padding: 10,
  },
  scrollView: {
    width: "100%",
    marginLeft: 25,
    marginTop: 10,
  },
});
