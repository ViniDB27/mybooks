import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  topBar: {
    height: 80,
    width: "100%",
    backgroundColor: "#0Af",
    alignItems: "center",
    justifyContent: "center",
    position: "relative",
  },
  title: {
    fontSize: 26,
    color: "#fff",
    fontWeight: "bold",
  },
});
