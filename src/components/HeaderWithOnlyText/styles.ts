import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  topBar: {
    height: 150,
    width: "100%",
    backgroundColor: "#0Af",
    alignItems: "center",
    justifyContent: "center",
    paddingTop: 35,
    position: "relative",
  },
  title: {
    fontSize: 26,
    color: "#fff",
    fontWeight: "bold",
  },
});
