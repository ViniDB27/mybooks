import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  topBar: {
    width: "100%",
    backgroundColor: "#0Af",
    alignItems: "center",
    justifyContent: "center",
    position: "relative",
  },
  title: {
    fontSize: 26,
    color: "#fff",
    fontWeight: "bold",
  },
});
